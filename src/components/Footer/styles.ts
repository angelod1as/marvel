import styled from 'styled-components'

export const Wrapper = styled.footer`
  font-size: 0.7em;
  color: white;
  opacity: 0.5;

  position: fixed;
  bottom: 10px;
  left: 0;
  width: 100%;
  text-align: center;

  a {
    color: white;
  }
`
